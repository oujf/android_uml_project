class VelocityInterpolator
!!!3707397.java!!!	VelocityInterpolator(in durationSeconds : float, in velocity : float, in diff : float)
            mDurationSeconds = durationSeconds;
            mVelocity = velocity;
            mDiff = diff;
!!!3707525.java!!!	getInterpolation(in input : float) : float
            float time = input * mDurationSeconds;
            return time * mVelocity / mDiff;
