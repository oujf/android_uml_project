class CapabilitiesBuilder
!!!865925.cpp!!!	addProfileLevel(in profile : uint32_t, in level : uint32_t) : void
    ProfileLevel profileLevel;
    profileLevel.mProfile = profile;
    profileLevel.mLevel = level;
    mProfileLevels.push_back(profileLevel);
!!!866053.cpp!!!	addColorFormat(in format : uint32_t) : void
    mColorFormats.push(format);
!!!866181.cpp!!!	addFlags(in flags : uint32_t) : void
    mFlags |= flags;
