class BufferTracker
!!!505093.cpp!!!	mergeFence(in with : sp<Fence>) : void
    mMergedFence = Fence::merge(String8("Camera3StreamSplitter"), mMergedFence, with);
!!!505221.cpp!!!	decrementReferenceCountLocked() : size_t
    if (mReferenceCount > 0)
        --mReferenceCount;
    return mReferenceCount;
