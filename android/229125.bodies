class EffectBufferHalInterface
!!!621701.cpp!!!	allocate(in size : size_t, inout buffer : sp<EffectBufferHalInterface>) : status_t
    *buffer = new EffectBufferHalLocal(size);
    return OK;
!!!621829.cpp!!!	mirror(inout external : void, in size : size_t, inout buffer : sp<EffectBufferHalInterface>) : status_t
    *buffer = new EffectBufferHalLocal(external, size);
    return OK;
