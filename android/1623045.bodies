class SessionRequest
!!!10875653.java!!!	SessionRequest(in userId : int, in locale : String, inout tsListener : ITextServicesSessionListener, inout scListener : ISpellCheckerSessionListener, inout bundle : Bundle)
            mUserId = userId;
            mLocale = locale;
            mTsListener = tsListener;
            mScListener = scListener;
            mBundle = bundle;
