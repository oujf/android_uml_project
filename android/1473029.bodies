class TextRenderNode
!!!9404677.java!!!	TextRenderNode(in name : String)
            renderNode = RenderNode.create(name, null);
            isDirty = true;
            needsToBeShifted = true;
!!!9404805.java!!!	needsRecord() : boolean
            return isDirty || !renderNode.isValid();
