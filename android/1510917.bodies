class PssAggr
!!!9869317.java!!!	add(in newPss : long, in newSamples : long) : void
            pss = (long)( (pss*(double)samples) + (newPss*(double)newSamples) )
                    / (samples+newSamples);
            samples += newSamples;
