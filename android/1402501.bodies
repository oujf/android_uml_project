class LogPrinter
!!!8462725.java!!!	LogPrinter(in priority : int, in tag : String)
        mPriority = priority;
        mTag = tag;
        mBuffer = Log.LOG_ID_MAIN;
!!!8462853.java!!!	LogPrinter(in priority : int, in tag : String, in buffer : int)
        mPriority = priority;
        mTag = tag;
        mBuffer = buffer;
!!!8462981.java!!!	println(in x : String) : void
        Log.println_native(mBuffer, mPriority, mTag, x);
