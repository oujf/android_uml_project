class ColorMatrixEvaluator
!!!11509509.java!!!	evaluate(in fraction : float, inout startValue : float [[]], inout endValue : float [[]]) : float
            for (int i = 0; i < mResultMatrix.length; i++) {
                mResultMatrix[i] = MathUtils.lerp(startValue[i], endValue[i], fraction);
            }
            return mResultMatrix;
