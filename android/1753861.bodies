class H
!!!12356869.java!!!	handleMessage(inout msg : Message) : void
            switch(msg.what) {
                case SHOW:
                    handleShow();
                    break;
                case HIDE:
                    handleHide();
                    break;
            }
