class ClearReceiver
!!!3106053.java!!!	onReceive(inout context : Context, inout intent : Intent) : void
            if (ACTION_CLEAR.equals(intent.getAction())) {
                Dependency.get(TunerService.class).clearAll();
            }
