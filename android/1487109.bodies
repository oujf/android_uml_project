class SelectionResult
!!!9620101.java!!!	SelectionResult(in start : int, in end : int, inout classification : TextClassification)
            mStart = start;
            mEnd = end;
            mClassification = Preconditions.checkNotNull(classification);
