class DnsServerEntry
!!!10529925.java!!!	DnsServerEntry(inout address : InetAddress, in expiry : long)
        this.address = address;
        this.expiry = expiry;
!!!10530053.java!!!	compareTo(inout other : DnsServerEntry) : int
        return Long.compare(other.expiry, this.expiry);
