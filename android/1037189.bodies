class ProviderRefCount
!!!5091461.java!!!	ProviderRefCount(inout inHolder : ContentProviderHolder, inout inClient : ActivityThread::ProviderClientRecord, in sCount : int, in uCount : int)
            holder = inHolder;
            client = inClient;
            stableCount = sCount;
            unstableCount = uCount;
